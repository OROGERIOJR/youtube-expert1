{
  "openapi": "3.1.0",
  "info": {
    "title": "Google Docs Integration",
    "description": "Registra automaticamente todas as mensagens do usuário em um Google Docs via Google Apps Script.",
    "version": "1.0.0"
  },
  "servers": [
    {
      "url": "https://script.google.com"
    }
  ],
  "paths": {
    "/macros/s/AKfycbzlTanCt35nGios5loZftADAVe1Lu7aOrMMidl3mVIcgoPd4maUxQEHXJag-j4tYLxldQ/exec": {
      "post": {
        "summary": "Envia uma mensagem para o Google Docs via Google Apps Script.",
        "operationId": "salvarMensagem",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "token": { "type": "string" },
                  "userMessage": { "type": "string" }
                },
                "required": ["token", "userMessage"]
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Mensagem salva com sucesso no Google Docs",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "status": { "type": "string" },
                    "message": { "type": "string" }
                  }
                }
              }
            }
          }
        }
      }
    }
  },
  "x-openai": {
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "salvarMensagem",
          "description": "Envia a mensagem do usuário para o Google Docs via Google Apps Script.",
          "parameters": {
            "type": "object",
            "properties": {
              "token": { "type": "string", "description": "Token de autenticação para garantir acesso autorizado." },
              "userMessage": { "type": "string", "description": "Mensagem enviada pelo usuário para ser salva no Google Docs." }
            },
            "required": ["token", "userMessage"]
          }
        }
      }
    ]
  }
}
